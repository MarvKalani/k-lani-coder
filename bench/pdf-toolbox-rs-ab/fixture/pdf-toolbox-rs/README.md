# PDF Toolbox RS

This crate is the Rust-first core milestone for the PDF toolbox benchmark.

The first implementation must model and validate merge jobs, build
backend-neutral operation plans, and provide a deterministic CLI smoke path. It
must not rewrite real PDF bytes yet.
