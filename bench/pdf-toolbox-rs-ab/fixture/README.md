# PDF Toolbox Fixture

This fixture contains the same start state for both benchmark modes.

- `reference/` contains selected docs from the original browser PDF converter.
- `pdf-toolbox-rs/` is the Rust scaffold to implement.

The benchmark task must change only files under `pdf-toolbox-rs` that are
listed in `product_spec.md`.
