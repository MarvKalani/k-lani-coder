# Copy to .env and fill in. NEVER commit the real .env.
# The key lives ONLY here and inside the proxy process — the agent
# frontend gets a dummy and the dashboard masks it everywhere.
MKLANI_UPSTREAM_URL=https://api.z.ai/api/coding/paas/v4/chat/completions
MKLANI_UPSTREAM_API_KEY=put-your-provider-key-here
MKLANI_UPSTREAM_MODEL=glm-5.1
